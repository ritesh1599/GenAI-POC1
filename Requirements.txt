# LangChain version pinned to compatible one (supports text_splitter)
langchain==0.0.335

# Core and text-splitter packages not needed for 0.0.335 version
# (these only apply for new 0.1.x+ structure)
# langchain-core
# langchain-text-splitters

# Other dependencies
tiktoken
pydantic
requests
faiss-cpu
numpy
sentence-transformers
boto3
python-dotenv
openai
google-generativeai
pytest
